{
  "name": "qa-agent-runner",
  "private": true,
  "version": "0.2.0",
  "description": "Deterministic executor for QA checks — attaches to your real Chrome over CDP so checks run at machine speed with your logged-in session.",
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright-core": "^1.45.0"
  }
}
